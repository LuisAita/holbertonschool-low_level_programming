#include "main.h"

/**
 * print_most_numbers - prints #0 - 9
 * !(2 || 4).
 * Return: Faceless Void
 */
void print_most_numbers(void)
{
	int c;

	for (c = 48; c <= 57; ch++)
	{
		if (c != 50 && c != 52)
			_putchar(c);
	}
	_putchar(10);
}
